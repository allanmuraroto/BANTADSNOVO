import { FiltroDeBuscaPipe } from './filtro-de-busca.pipe';

describe('FiltroDeBuscaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroDeBuscaPipe();
    expect(pipe).toBeTruthy();
  });
});
